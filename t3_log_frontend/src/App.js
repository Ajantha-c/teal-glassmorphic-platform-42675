import React from "react";
import "./App.css";
import LoginPage from "./pages/LoginPage";

// PUBLIC_INTERFACE
function App() {
  /** Main React entry rendering the login UI (no auth, UI only). */
  return <LoginPage />;
}

export default App;
